import 'package:flutter/cupertino.dart';

class CareTakerInformation extends StatelessWidget {
  const CareTakerInformation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text('Cuidador responsável: ')],
    );
  }
}
