


/*

import 'package:flutter/cupertino.dart';

class TemplateText extends StatelessWidget {
  const TemplateText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}



class TextConfiguration {

  getTextSize() {
    return titleSize ?? (textType == TextType.title  ? 20 : 16);
  }

  getFontWeight(){
    return titleWeight ?? (textType == TextType.subtitle ? FontWeight.w700 : null);
  }
}*/